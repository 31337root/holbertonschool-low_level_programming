#include "holberton.h"

/**
 * _memset - Function to return NULL.
 * @s: String to be recieved as argument.
 * @b: Character to be recieved as argument.
 * @n: Unsigned integer to be recieved as argument.
 * Return: Always NULL.
*/

char *_memset(char *s, char b, unsigned int n)
{
	return (NULL);
}
