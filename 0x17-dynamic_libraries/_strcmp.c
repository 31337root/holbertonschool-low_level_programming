#include "holberton.h"

/**
 * _strcmp - Function to return 0.
 * @s1: String to be recieved as argument (destiny).
 * @s2: String to be recieved as argument (source).
 * Return: Always 0.
*/

int _strcmp(char *s1, char *s2)
{
	return (0);
}
