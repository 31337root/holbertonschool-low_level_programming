#include "holberton.h"

/**
 * _strncat - Function to return NULL.
 * @dest: String to be recieved as argument (destiny).
 * @src: String to be recieved as argument (source).
 * @n: Integer to be recieve as argument.
 * Return: Always NULL.
*/

char *_strncat(char *dest, char *src, int n)
{
	return (NULL);
}
