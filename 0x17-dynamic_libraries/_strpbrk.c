#include "holberton.h"

/**
 * _strpbrk - Function to return NULL.
 * @s: String to be recieved as argument.
 * @accept: String to be recieved as argument.
 * Return: Always NULL.
*/

char *_strpbrk(char *s, char *accept)
{
	return (NULL);
}
