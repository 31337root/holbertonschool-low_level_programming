#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 * @num: value to process
 * Return: the value indicted
*/

int _abs(int num)
{
	if (num < 0)
	{
		num = num * -1;
		return (num);
	}
	else
	{
		return (num);
	}
}
