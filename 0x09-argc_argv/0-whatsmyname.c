#include <stdio.h>

/**
 * main - Entry point.
 * @argc: The number of command line arguments.
 * @argv: An awway containing the program command line arguments.
 * Return: Success Always (0).
*/

int main(int argc __attribute__((unused)), char *argv[])
{

	printf("%s\n", argv[0]);

	return (0);
}
