#include "holberton.h"

/**
 * *_memset - Return 0.
 * @s: Entry character.
 * @b: Entry character.
 * @n: Entry unsigned integer.
 * Return: Success Always (0).
*/

char *_memset(char *s, char b, unsigned int n)
{
	s = s;
	b = b;
	n = n;

	return (s);
}
