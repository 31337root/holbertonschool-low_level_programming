#include "holberton.h"

/**
 * _strspn - Return 0.
 * @s: Entry character.
 * @accept: Entry character.
 * Return: Success Always (0).
*/

unsigned int _strspn(char *s, char *accept)
{
	s = s;
	accept = accept;

	return (0);
}
